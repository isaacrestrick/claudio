services:
  - type: web
    name: claudio
    runtime: python
    buildCommand: pip install ./mcp
    startCommand: python mcp/server.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: MCP_TRANSPORT
        value: http
